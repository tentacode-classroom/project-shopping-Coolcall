{% extends 'base.html.twig' %}

{% block content%}



    <!-- Page Content -->
    <div class="container">

      <!-- Team Members Row -->
      <div class="row">
        <div class="col-lg-12">
          <h2 class="my-4">Liste des lamasticots</h2>
        </div>
        <div class="col-lg-4 col-sm-6 text-center mb-4">
          <img class="rounded-circle img-fluid d-block mx-auto" src="{{ asset('/img/' ~ lamasticot.image) }}" />
          <h3>{{ lamasticot.name}}
            <small>{{ lamasticot.name }}</small>
          </h3>
          <p>{{ lamasticot.size}} cm {{lamasticot.cost}} €</p>
          <p>{{ lamasticot.getColor().getColor()}} {{lamasticot.object}} </p>
          <p> {{ lamasticot.viewcounter }} vues</p>
        </div>
      </div>

    </div>
{%endblock%}
